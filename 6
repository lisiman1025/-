function addSum(x,y){
    var sum = x;
        if(y){
            return (sum + y);
        }
        else
        {
            var addSum2 = function(z){
                return (sum + z);
            }
            return add2;
        }
    }
